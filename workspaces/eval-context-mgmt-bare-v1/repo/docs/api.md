# Synthetic API doc

Same fixture as in `eval-context-mgmt-configured-v1/repo/docs/api.md` so the
agent sees identical content; the only meaningful difference between
the two workspaces is the presence/absence of `.cursor/` configs.
